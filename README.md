# SingLife Exam with React Native and Expo Library

To use:

    * npm install
        - to install dependencies
    * npx expo start
        - to start the metro
        - scan the QR Code

Note: Download Expo Go in your mobile to run the project.
